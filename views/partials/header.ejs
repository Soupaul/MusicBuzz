<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>MusicBuzz</title>

    <!-- Google Fonts  -->
    <link href="https://fonts.googleapis.com/css2?family=PT+Sans:wght@400;700&display=swap" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Lobster&display=swap" rel="stylesheet">

    <!-- Bootstrap CDN -->
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css" integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh" crossorigin="anonymous">

    <!-- Custom CSS -->
    <link rel="stylesheet" href="/css/styles.css">

    <style>

        #searchBar{
            
            width: 400px;
            transform: scale(1);
            transition: transform 0.5s; 

        }

        #searchBar:hover{

            transform: scale(1.05);

        }

    </style>
    
</head>
<body>

    <div class="bg-holder">

       <% if(showNavbar){ %>

            <nav style="z-index: 1;" class="navbar navbar-expand-lg navbar-dark">
                <a class="navbar-brand" style="color: #fed000; font-family: 'Lobster',cursive; font-size: 30px;" href="/">MusicBuzz</a>
                <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarNav">
        
                    <form id="navbarSearch" action="/search" method="POST" class="form-inline my-2 my-lg-0">
                        <input id="searchBar" class="form-control mr-sm-2" type="search" name="searchQuery" placeholder="Search a song or artist" aria-label="Search">
                        <button style="margin-left: 15px;" class="btn btn-outline-warning my-2 my-sm-0" type="submit"><i class="fas fa-search"></i></button>
                    </form>
        
                    <ul class="navbar-nav ml-auto">
        
                        <!-- <li class="nav-item">
        
                            <% let link = auth ? "/logout" : "/login" %>
        
                        <a class="nav-link" href=<%= link %>>
                            
                            <% if(!auth){ %>
                                Login
                            <% }else{ %>
                                Logout
                            <% } %>
                        </a>
                        </li> -->

                        <li class="nav-item">

                            <a class="nav-link" href="/login">
                            
                                <button class="btn btn-md btn-danger" style="font-size: 20px;">Login with <i class="fab fa-google-plus-square"></i></button>

                            </a>

                        </li>
                    </ul>
        
        
                </div>
            </nav>

        <% } %>

        <div class="bg"></div>
